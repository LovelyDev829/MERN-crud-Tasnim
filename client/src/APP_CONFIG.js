export const APP_CONFIGURATION = {
  DB_PROTOCOL: "http",
  DB_HOST: "localhost",
  DB_PORT: 5000,
};

export const user = {
  userID: "admin",
  password: "admin",
};

export let authenticated = false;
